# AGENTS.md

## 目的

本文件用于约束 Codex 在本仓库中的编程方式。

仓库维护者是一名软件初学者，因此所有代码修改都应优先考虑：

- 简洁
- 清晰
- 易读
- 易测试
- 易讲解

不追求过度抽象，不追求炫技式写法，不为了“看起来高级”而增加理解成本。

## 编程原则

### 1. 优先写简单直接的代码

- 优先选择最容易读懂、最容易验证的实现。
- 先解决问题，再考虑抽象。
- 如果一个功能用普通函数就能说清楚，不要强行拆成复杂类层级。
- 不要为了“通用性”提前设计大量暂时用不到的接口。

### 2. 不要过度设计

- 不要引入与当前需求无关的设计模式。
- 不要为了未来可能的需求，提前增加复杂扩展点。
- 不要把简单逻辑拆成过多文件、过多类、过多中间层。
- 如果抽象不能明显降低重复或提升可读性，就保持展开写法。

### 3. 代码要适合初学者阅读

- 使用清晰直白的命名。
- 函数尽量短小，单一职责明确。
- 控制嵌套层级，避免过深的 `if/else`。
- 复杂逻辑前可以添加简短中文注释，解释“为什么这样做”。
- 避免不必要的高级技巧，比如过度链式调用、晦涩的一行写法、过强的元编程。

### 4. 修改要尽量局部

- 优先做小而明确的修改。
- 不要在完成当前任务时顺手大规模重构无关代码。
- 如果发现更大的结构问题，可以在说明中指出，但不要擅自扩大改动范围。

### 5. 保持行为可验证

- 新增代码时，要同时提供对应的测试方法。
- 修复 bug 时，要说明如何复现问题、如何验证修复。
- 如果仓库里没有完整自动化测试框架，至少提供手动测试步骤。

## 测试要求

### 1. 新增代码必须附带测试说明

当新增一个功能、模块或明显的新分支逻辑时，必须同时提供以下至少一项：

- 自动化测试代码
- 可直接执行的命令行验证方法
- 明确的手动测试步骤

### 2. 测试说明要具体

测试说明不能只写“自行运行检查”或“应该正常工作”，而要写清楚：

- 测试前置条件
- 执行步骤
- 预期结果

### 3. 优先可重复验证

- 如果适合写自动化测试，优先补自动化测试。
- 如果暂时不适合写自动化测试，要提供稳定、明确、可重复的手动验证步骤。

## 输出要求

Codex 在完成代码修改后，说明中应尽量包含：

1. 改了什么
2. 为什么这样改
3. 如何测试

如果本次没有添加自动化测试，也要明确说明原因，并给出手动测试方法。

## 重大修改记录要求

### 1. 重大修改后必须记录日志

当发生重大修改时，除了在对话中做简要总结，还要把修改摘要追加记录到 `docs/build-log.md`。

### 2. 记录格式必须统一

重大修改记录应尽量接近正式软件的更新日志格式。

推荐格式：

`## 版本号 - 日期 时间`

下面使用 1 到 3 条简短条目说明本次修改，例如：

- `优化了哪些功能或代码`
- `修复了哪些问题`
- `补充了哪些测试或验证方式`

### 3. 说明要简短明确

- 只记录本次重大修改的核心内容。
- 说明应让人快速看懂“改了什么”。
- 不写空泛描述，比如“做了一些优化”。

## 风格要求

- 默认使用简单、朴素、工程上稳定的实现方式。
- 代码示例和说明优先写得让初学者一眼能看懂。
- 能用 10 行清楚解决的问题，不要写成 30 行“通用框架”。
- 能用一个函数解决的问题，不要拆成过多跳转层级。

## 适用范围

本文件适用于本仓库中的所有代码、文档和相关修改建议。
